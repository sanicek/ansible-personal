# TODO

- [x] Deploy a global `orchestrator_append.md` through the Arch OpenCode role that enforces strict built-in specialist-lane ownership and requires an explicit lane check before direct edits.
- [x] Check whether other Molecule scenarios need to refresh the Pacman package cache during preparation.
- [x] Add a notification setting for OpenCode (e.g. desktop notification on task completion).
- [x] Create a Copilot-based OMO (oh-my-opencode-slim) profile.
- [ ] Discover and evaluate skills from the skill registry that match the current repo scope (Ansible collections, personal workstation config).
- [ ] Create a ComfyUI playbook/role for Arch (image generation / Stable Diffusion tooling).
