# fedora_molecule

Installs local Fedora prerequisites for running Podman-backed Molecule scenarios from this repository.

Python package dependencies are intentionally kept in the repository-level `requirements-dev.txt` and should be installed into a local virtual environment.
