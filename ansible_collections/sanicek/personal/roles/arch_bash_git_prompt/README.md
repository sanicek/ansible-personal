# arch_bash_git_prompt

Installs and configures bash-git-prompt on Arch Linux hosts. The role installs a custom `Flexoki` theme with the same layout and status behavior as the upstream `Solarized` theme, using bright cyan for the path and yellow for the time from the terminal palette.
