# arch_podman

Installs Podman on Arch Linux.

On WSL only, configures user-level Podman defaults for rootless Molecule builds: cgroupfs cgroup management, file-backed event logging, and chroot build isolation.
